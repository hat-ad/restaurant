import React from "react";
import Lottie from "react-lottie";
import { defaultLoaderOptions } from "../../common/config";

const index = () => (
  <Lottie options={defaultLoaderOptions} height={250} width={250} />
);

export default index;
